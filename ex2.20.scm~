(define (same-parity a .b)
  (let ((pari (remainder a 2)))
    (define (rec-func ls)
      (if (null? ls)
	  '()
	  (if (= (remainder (car ls) 2) pari)
	      (cons (car ls) (rec-func (cdr ls)))
	      (rec-func (cdr ls)))))
    (rec-func b)))
